# TUI screenshots with the raven — 2026-09-17

`tui-raven-rich.svg` and `tui-raven-plain.svg` replace the ferret-era pair the README
linked (`tui-v2-rich.svg`, `tui-v2-plain.svg`, which stay as part of the
[2026-09-15 recording](2026-09-15-tui-v2-live.md)). Same method as that pair:
`ProofpathApp(load_config(), ui.build(force_terminal=True), theme=…)` under
`app.run_test(size=(100, 54))` with the **real `Scheduler`**, `/check
tests/data/draft-live.md`, polled until the run was `done`, then
`app.save_screenshot()`.

- version `0.4.2`, macOS, CoreML; cache warm for the sources, so the network stages
  are attributed to `cache` and the footer says `0 API calls`
- OpenAlex answered 429 during the ghost check (`retry after 65567s`); the finding
  says so, as it should — the ghost call stood on arXiv and Open Library
- the run: 7 refs, 1 ghost, 2 unsupported, 4 ok; coverage 72/14/14 %

The Braille raven's dots render with whatever glyphs the viewer's fallback font has;
a terminal with a monospace font that carries Braille (Menlo, JetBrains Mono,
Cascadia, Fira Code) draws them evenly.
